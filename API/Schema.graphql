scalar Date

schema {
  query: Query
}

type Query {
  GetRecipes: [Recipe]!
}

type Recipe {
  id: ID!
  name: String
  photos: [String]!
  description: String
  ingredients: [RecipeIngredient]!
  steps: [String]
  tags: [String]
}

type RecipeIngredient {
  ingredient: String!
  amount: Float!
  unit: String
}

type Pantry {
  id: ID!
  items: [PantryItem]!
}

type PantryItem {
  ingredient: String!
  amount: Float!
  unit: String
  expirationDate: Date
}

type User {
  id: ID!
  email: String!
  password: String!
  claims: [String]
}

type Token {
  access: String!
  refresh: String!
}
